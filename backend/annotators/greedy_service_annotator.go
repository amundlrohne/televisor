package annotators

// Check services that have a singular function
// Microservices with very limited functionalities (e.g., a microservice serving only one static HTML page).

func GreedyServiceAnnotator() {
	return
}
